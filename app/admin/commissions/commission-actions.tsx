"use client"; import { updateCommissionStatus } from "@/server/actions"; export default function CommissionActions({id,status}:{id:string,status:string}){if(status==="PAID"||status==="CANCELLED")return null;return <div className="actions">{status==="PENDING"&&<button className="btn alt" onClick={()=>updateCommissionStatus(id,"APPROVED")}>Setujui</button>}<button className="btn" onClick={()=>updateCommissionStatus(id,"PAID")}>Tandai Dibayar</button></div>}
