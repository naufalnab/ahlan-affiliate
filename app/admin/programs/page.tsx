import { db } from "@/lib/db"; import { money } from "@/lib/format"; import { AdminShell } from "@/components/ui"; export default async function Programs(){const programs=await db.program.findMany();return <AdminShell><div className="pagehead"><p className="eyebrow">Katalog</p><h1>Program Ahlan</h1></div><div className="grid cards">{programs.map(p=><article className="card" key={p.id}><h2>{p.name}</h2><p className="muted">{p.description}</p><p className="metric">{money(p.price)}</p><span className="badge">{p.active?"Aktif":"Nonaktif"}</span></article>)}</div></AdminShell>}
