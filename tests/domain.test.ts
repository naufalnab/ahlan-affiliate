import { describe,expect,it } from "vitest"; import { calculateCommission,conversion,normalizePhone,simulation } from "../lib/domain";
describe("business logic",()=>{it("normalizes Indonesian phone numbers",()=>{expect(normalizePhone("0812-1234-8454")).toBe("6281212348454");expect(normalizePhone("+6281212348454")).toBe("6281212348454")});it("calculates commissions",()=>{expect(calculateCommission(500000,"FIXED",75000,10)).toBe(75000);expect(calculateCommission(500000,"PERCENTAGE",75000,10)).toBe(50000)});it("calculates conversion safely",()=>{expect(conversion(29,41)).toBe(71);expect(conversion(0,0)).toBe(0)});it("simulates program economics",()=>{expect(simulation(500000,100,3,30,"PERCENTAGE",10)).toEqual({leads:300,students:90,gross:45000000,cost:4500000,net:40500000})})});
